# Copy this file to .env.local in the same directory, then replace the
# placeholder with your real key. The .env.local file is git-ignored so
# the secret never leaves your machine.
#
# Get a key at https://www.hedra.com → Dashboard → API Keys.
HEDRA_API_KEY=replace-me-with-your-real-key
